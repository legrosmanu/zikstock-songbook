class ZikResource {

    constructor(url, artist, title, addedBy, tags) {
        this.url = url;
        this.artist = artist;
        this.title = title;
        this.addedBy = addedBy;
        this.tags = tags;
    }

}

module.exports = ZikResource;
